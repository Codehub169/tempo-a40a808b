# Environment variables for ReTech Backend

# Server Configuration
PORT=9000

# JWT Configuration
# IMPORTANT: Change this to a strong, random string for production!
JWT_SECRET="your-super-secret-jwt-key-please-change-me"
JWT_EXPIRES_IN="7d"

# Database Configuration
# The path to the SQLite database file, relative to the backend root directory
DATABASE_URL="./data/database.sqlite"

# Frontend URL (for CORS if needed, though serving static files might bypass this for GET)
# FRONTEND_URL=http://localhost:5173 # Example if frontend runs on different port during dev
